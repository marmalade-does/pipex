#include "pipex.h"

int main(int argc, char *argv[], char **envp)
{
	int pip1[2];
	int pip2[2];
	// char **splited;
	size_t i;

	if (argc <  )
	// input check
	// check if here_doc mode
	// iff here_doc
	// -- 6 args?
	// -- make the i = 3;
	// make the oufile append mode
	// calll the here doc function

	// i = 2
	// outfile = O_TRUNC
	// infile = the infile
	// have the pipe initialisations in the ft_chil proces?? (I think)

	// while look over the children
	// have a final dup2(fd[0], STDDOUT_FD);
	// return (0); success
}

void ft_child()
{}